# Neon Postgres connection string
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DBNAME?sslmode=require"

# Auth
JWT_SECRET=                              # generate via: openssl rand -base64 48
SITE_ACCESS_CODE=RRForecast2026          # site-wide first-layer gate (dev placeholder; override in prod)

# Seed (used by prisma/seed.ts only — first deploy bootstrap)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123                  # dev placeholder; override in prod, rotate after first login
ADMIN_DISPLAY_NAME=Administrator
